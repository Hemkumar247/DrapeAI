import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.drape.virtualtryon',
  appName: 'drape-virtual-try-on',
  webDir: 'dist'
};

export default config;
